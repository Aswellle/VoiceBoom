# PC端实时流式语音输入法软件 PRD

**产品名称**：VoiceBoom AI —— 实时流式智能语音输入法  
**文档版本**：V1.0  
**创建日期**：2026-08-09  
**作者**：产品团队  
**状态**：已定稿（待评审）

---

## 修订历史

| 版本 | 日期       | 修订内容                          | 作者       |
| ---- | ---------- | --------------------------------- | ---------- |
| V0.5 | 2026-07-25 | 初始草案，技术选型与功能框架      | 产品架构组 |
| V1.0 | 2026-08-09 | 完善为专业PRD，补充验收标准与风险 | 产品团队   |

---

## 1. 项目概述

### 1.1 产品名称
**VoiceBoom AI —— 实时流式智能语音输入法**

### 1.2 产品定位
一款面向 **Windows / macOS** 平台的 **低延迟实时语音转文字输入工具**。

用户通过快捷键唤醒麦克风后，可以连续自然讲话，系统实时将语音流转换为文字，并以悬浮窗口形式展示最新识别结果。产品聚焦于“**边说边出字、所见即所得**”的核心体验，同时保持极低的系统资源占用和苹果级的视觉美学。

核心体验宣言：
> 像 Apple macOS 原生交互一样简洁优雅，同时具备 AI 时代实时语音输入能力。

### 1.3 产品目标
- **主要目标**：提供延迟 ≤ 500ms 的实时流式语音转文字体验，替代平台自带听写功能及主流第三方语音输入工具。
- **次要目标**：支持云端与本地双引擎可插拔，满足不同网络环境和隐私需求；未来版本实现系统级文本直接注入任意应用。
- **商业目标**：建立“下一代 AI 原生语音输入层”品牌认知，为后续的 AI 润色、多语言翻译等高级功能打造生态入口。

### 1.4 目标用户
- **高频文字工作者**：记者、作家、产品经理、学生，需要快速记录想法或会议内容。
- **多语言沟通者**：需要在中/英/日/韩等语言间无缝切换语音输入的用户。
- **对隐私有要求的用户**：希望在不联网的情况下使用本地模型进行语音转文字。
- **效率工具爱好者**：习惯使用 Raycast、Alfred 等系统效率工具，追求键盘与语音融合的交互方式。

### 1.5 竞品与替代关系
VoiceBoom AI 目标替代以下工具场景：
- Windows 自带语音输入（Win+H）
- macOS Dictation（听写功能）
- 基于 Whisper GUI 的转写工具（如 Buzz、MacWhisper）
- 会议实时转写软件（如飞书妙记、通义听悟等会议转写模式）
- 传统语音输入法（如讯飞语音输入法的PC版）

相比于以上工具，VoiceFlow AI 以 **低延迟流式输出、极简悬浮窗交互、可插拔模型架构、跨平台一致的苹果式设计** 形成差异化优势。

---

## 2. 产品范围与版本规划

### 2.1 MVP（V1.0） —— 核心体验闭环
**周期**：4 - 6 周

**功能集**：
- Tauri 桌面应用壳，支持 Windows 10+ 与 macOS 13+
- 音频采集（麦克风权限、采样率配置）
- 云端流式 ASR 集成（默认提供 OpenAI Whisper API 与 Deepgram 适配器）
- 悬浮文字窗口，实时展示识别结果
- 最大展示长度限制与文本裁剪逻辑
- macOS 风格毛玻璃 UI 与 Framer Motion 入场/过渡动画
- 全局快捷键唤醒（按住说话，松开停止）
- 基础的设置页面（语言选择、最大字符数、模型切换）

### 2.2 V1.5 —— 本地化与增强
- 增加本地离线引擎：Whisper.cpp（跨平台）与 FunASR/SenseVoice（中文优化）
- 多语言自动检测与手动切换
- 识别历史记录存储在 SQLite，支持搜索与复制
- 主题系统：跟随系统（亮/暗），或强制亮色/暗色模式
- 基础的系统输入注入功能：将文字直接发送到当前活动应用

### 2.3 V2.0 —— AI 增强层
- **AI 润色模式**：对识别文本进行口语修正、标点恢复、语气优化。
  例如输入口语：“那个就是我们那个项目然后大概下周完成”，自动优化为：“我们的项目预计将在下周完成。”
- 专业模式：针对法律、医学、编程等场景定制术语库。
- 多设备剪贴板同步（通过登录账户同步剪贴板文本历史）。

---

## 3. 技术架构与设计

### 3.1 架构总览
采用 **Tauri 2.0 (Rust) 作为桌面壳，React 19 + TypeScript 构建前端 UI，Rust 后端负责音频捕获与 ASR 流管理**。

音频管线示意图：

```
Microphone
    │
    ▼
Rust Audio Capture (CPAL, 16kHz, Mono)
    │
    ▼
Audio Buffer & VAD (optional)
    │
    ▼
Streaming ASR Engine (WebSocket → Cloud / Local Engine)
    │
    ▼
Text Event (partial/final)
    │
    ▼
Tauri Event → React Frontend (Zustand Store)
    │
    ▼
Framer Motion Animated Floating Window
```

### 3.2 关键技术选型理由

| 模块     | 选型                    | 理由                                                         |
| -------- | ----------------------- | ------------------------------------------------------------ |
| 桌面框架 | Tauri 2.0               | 体积小于10MB，内存<50MB；原生系统能力（快捷键、系统托盘、权限管理）；Rust 后端性能优越，适合实时音频处理。 |
| 前端 UI  | React 19 + Tailwind v4  | 组件化生态丰富，Tailwind 快速实现设计系统，Framer Motion 提供声明式动画，轻松达到 macOS 级动画效果。 |
| 状态管理 | Zustand                 | 轻量、无模板代码，与 Tauri 事件系统集成简单。                |
| 音频采集 | CPAL                    | 支持 Windows WASAPI 与 macOS CoreAudio 的低延迟采集，Rust 生态首选。 |
| 实时通信 | WebSocket (Tungstenite) | Rust 端管理 WebSocket 长连接，与 ASR 云服务实时传输音频流；对离线引擎通过本地进程通信（如 stdin/stdout 或 FFI）。 |
| ASR 引擎 | 云端：OpenAI / Deepgram | 第一阶段确保低门槛、高质量流式识别；后续扩展本地引擎支持。   |
| 数据库   | SQLite (via rusqlite)   | 客户端轻量存储设置、历史记录与模型配置；无需服务端依赖。     |
| 跨平台   | Tauri 原生跨平台        | 同一套代码覆盖 Windows 10+ 和 macOS 13+；Linux 后续支持。    |

### 3.3 UI 设计体系：macOS Glass UI

**设计原则**：毛玻璃质感（Glassmorphism），以 Apple macOS Sonoma / Tahoe 的视觉语言为参考，同时保持 Windows 端的清晰度。

**基本样式规范**：
- 背景：`rgba(255,255,255,0.72)` (亮色模式) 或 `rgba(0,0,0,0.65)` (暗色模式)
- 模糊：`backdrop-filter: blur(30px)`
- 阴影：`0 10px 40px rgba(0,0,0,0.15)`
- 圆角：`20px`
- 字体：macOS 使用 SF Pro Display，Windows 使用 Segoe UI Variable；字号默认 22px，行高 1.4

**动画规范**：
- 新文本入场：`fade in` + `slide from right (10px)`，时长 0.2s ease-out
- 旧文本淡出：透明度从 1.0 降至 0.3，时长 0.15s
- 窗口出现/消失：scale 0.8→1.0，opacity 0→1，弹性曲线 spring

---

## 4. 功能需求

### F001 实时语音转文字（核心功能）
**描述**：用户在按住快捷键说话时，系统实时捕获麦克风音频并通过 ASR 引擎生成文字。支持连续讲话，自动进行简单断句。

**输入**：用户语音（中/英/日/韩等）
**输出**：部分识别结果 (`partial`) 和最终识别结果 (`final`)，通过事件推送到前端。

**验收标准**：
- 语音输入开始后，首个文字片段在 500ms 内出现在悬浮窗。
- 最终结果在用户停止说话后 1s 内稳定输出（基于 VAD 或服务端标记）。
- 支持长时间连续说话（至少 5 分钟），不崩溃、不丢失上下文。
- 文字准确率：云端引擎下中文通用场景 > 95%（基于标准测试集）；本地引擎待优化。

### F002 实时流式展示窗口
**描述**：一个始终置顶的悬浮面板，显示最新的识别文本。窗口无标题栏，可鼠标拖拽移动位置；未激活时半透明，有识别内容时高亮。

**文本布局规则**（特色设计）：
- 最 **新** 识别的句子显示在 **最左侧/最上方**，旧句子逐渐向右/向下排列（类似聊天消息时间轴倒序）。
- 保持每个句子（由 `final` 事件结束）作为独立片段，视觉上以轻分割线或间距分隔。
- 当新句子出现时，已有的句子组向右滑动并降低不透明度（保持在 0.35）。

**验收标准**：
- 窗口无边框，背景应用毛玻璃效果，与系统桌面视觉融合。
- 按最新到最旧排列文本，且始终能够看到最近的 3~5 个句子。
- 窗口可被鼠标拖拽到任意位置，下次启动记住位置。

### F003 最大展示长度限制
**描述**：用户可在设置中配置“最大展示字符数”。当累计文本长度超过该限制时，从最旧的内容开始裁剪，并在保留内容尾部显示省略号“...”。

**逻辑规则**：
- 裁剪发生在 `final` 句子被添加且总长度超限时。
- 只裁剪被标记为 `final` 的完整句子，不裁剪 `partial` 的中间状态。
- 被裁剪掉的句子数据仍保存在历史记录中，仅影响前端显示。

**验收标准**：
- 默认最大长度 80 字符，可调整范围为 20-500。
- 超过长度后，最旧句子从 DOM 中移除，省略号显示自然。
- 调整配置后立即对当前显示生效。

### F004 实时滚动与入场动画
**描述**：为新出现的文本添加引人注目但不打扰的动画，增强“实时感”。

**效果细节**：
- 每个新句子（`final`）以 `fade in + slide from right` 方式入场。
- 正在进行的 `partial` 文本在其占位符中实时变化，无入场动画，但字符变化平滑。
- 所有动画使用 Framer Motion 的 `layout` 动画实现容器自动高度/宽度变化。

**验收标准**：
- 动画帧率 ≥ 60fps，不引起 UI 卡顿。
- 动画时间符合设计规范，无突兀跳变。
- 支持减弱动画选项（在设置中），满足无障碍需求。

### F005 快捷键唤醒（按住说话）
**描述**：全局热键注册，用户按住快捷键开始录音，松开停止。可与系统其他快捷键共存（如 Windows 的 Ctrl+Space 可能与输入法切换冲突，需提供备选）。

**默认键位**：
- Windows：`Ctrl + Space`（备用：`Ctrl + Shift + V`）
- macOS：`Command + Space`（需处理与 Spotlight 冲突，备用 `Option + Space`）
- 所有快捷键支持用户在设置中自定义修改。

**验收标准**：
- 当目标应用获得焦点时，快捷键能够全局拦截，即使 VoiceFlow 窗口非焦点也生效。
- 按住后 100ms 内开始音频捕获（避免捕获按键音）。
- 松开后 200ms 内停止录音并发送结束信号。
- 与主流应用（浏览器、IDE、Office）无冲突；提供冲突检测引导。

### F006 自动语言检测与切换
**描述**：用户可以设置语言为“自动检测”或指定单一语言。自动检测模式下，系统根据语音片段识别语种并调用相应模型。

**支持语种**：中文（普通话）、英语、日语、韩语；后续可扩展。

**验收标准**：
- 自动检测在用户说 1~2 个单词后即可正确切换，延迟不超过 1 秒。
- 手动选择语言后，强制使用该语言模型，不再自动切换。
- 语言切换状态在悬浮窗上以小图标指示（例如 CN / EN ）。

### F007 系统输入注入（V1.5+）
**描述**：将识别完成的最终文字直接输入到当前活动的文本区域，无需手动复制粘贴。类似系统输入法的上屏机制。

**技术方案**：
- Windows：使用 `SendInput` API 模拟键盘输入。
- macOS：使用 `CGEventPost` 模拟键盘事件。

**验收标准**：
- 用户通过特定手势（如双击悬浮窗或额外快捷键）触发上屏。
- 文本输入速度与手动键入无差异，无丢失字符。
- 能正确处理换行、标点符号。

---

## 5. 详细设计

### 5.1 悬浮窗口规范
| 属性     | 值                                         |
| -------- | ------------------------------------------ |
| 类型     | 无边框悬浮面板，始终置顶                   |
| 默认大小 | 宽度 600px，高度随内容变化，最小 90px      |
| 位置     | 默认屏幕顶部中央，可拖拽调整               |
| 颜色     | 亮色/暗色模式自适应，支持毛玻璃背景        |
| 字体     | macOS: SF Pro Display, Windows: Segoe UI   |
| 状态指示 | 录音红点 + 动态波形；待机状态显示“🎙 Ready” |

### 5.2 状态机与交互
**状态**：
- `idle`：窗口半透明，显示提示“按住快捷键说话”。
- `listening`：窗口不透明度提升，显示动态波形与红色●指示，实时刷新部分文字。
- `result`：松开按键后保持结果展示 2 秒，然后回到 `idle` 或显示更多结果（可配置）。

**交互操作**：
- 拖拽窗口：鼠标按住非文字区域直接拖动。
- 右键菜单：设置、历史记录、退出。
- 单击复制：点击某段句子复制到剪贴板，伴随视觉反馈。
- 双击上屏（V1.5+）：双击句子直接输入到之前焦点应用。

### 5.3 设置中心结构
设计为独立的 Tauri 窗口，按功能分标签页：

1. **语音**：麦克风选择、录音音量指示、VAD 灵敏度。
2. **AI 模型**：选择云服务（输入 API Key）或下载本地模型；语言设置。
3. **快捷键**：录音快捷键配置；上屏快捷键；冲突检测。
4. **显示**：最大字符数、字体大小、透明度、主题、窗口位置锁定。
5. **高级**：开机自启、代理设置、日志级别、数据导出。
6. **关于**：版本信息、开源许可、检查更新。

### 5.4 数据存储设计（SQLite）
数据库文件存储在 Tauri 应用数据目录。核心表结构：

- `settings`：键值对保存用户配置。
- `history`：记录每条识别结果（时间戳、文本、语言、引擎）。
- `shortcuts`：存储用户自定义快捷键映射。
- `model_config`：保存云服务密钥（加密存储）和本地模型路径。

---

## 6. 非功能性需求

### 6.1 性能指标
- **端到端延迟**（从用户说出字到文字显示）：≤ 500ms（云端，正常网络）；本地引擎 ≤ 300ms。
- **UI 刷新率**：保证 60fps，动画帧时间 ≤ 16ms。
- **CPU 占用**：空载 < 1%，录音+云端 ASR < 5%，本地模型推理视硬件而定（预期 < 15% 高端机型）。
- **内存占用**：Windows ≤ 100MB，macOS ≤ 80MB。
- **安装包大小**：< 15MB（不含模型），本地模型额外下载。

### 6.2 稳定性与可靠性
- 连续录音 30 分钟不出现音频丢帧或应用无响应。
- 网络断开时自动提示并停止录音，恢复后需手动重新开始。
- 音频设备变更（如插拔耳机）时自动切换，不崩溃。

### 6.3 安全性
- API 密钥等敏感信息使用系统凭据存储（macOS Keychain / Windows Credential Manager）。
- 所有网络通信使用 WSS 加密。
- 本地历史记录存储不加密，但提示用户隐私风险。

### 6.4 兼容性
- Windows 10 21H2 及以上，Windows 11。
- macOS 13 Ventura 及以上。
- Linux 发行版待后续支持（首选 Ubuntu 22.04+）。

### 6.5 可维护性与扩展性
- ASR 引擎通过抽象 trait 接口实现，增加新引擎只需实现 `StreamingAsrEngine` 接口。
- 前端组件解耦，展示窗口与设置窗口通过 Tauri 事件通信，可独立替换。

---

## 7. 用户场景与用例

### 用例 1：快速记录想法
**参与者**：产品经理小明
**前置条件**：VoiceBoom 已开启，默认使用云端 ASR。
**主流程**：
1. 小明在浏览器阅读文档时，灵感来了，按住 `Ctrl+Space` 开始说话。
2. 悬浮窗立即显示红色●和动态波形，开始实时输出文字。
3. 小明说完“这个需求可以增加一个暗黑模式”，松开按键。
4. 文字保留在窗口，小明点击文字一键复制，粘贴到 Notion 中。
5. 悬浮窗恢复半透明待机状态。

### 用例 2：双语会议记录
**参与者**：跨国团队中的开发者
**前置条件**：语言设置为“自动检测”。
**流程**：
1. 会议中，中英文轮番切换，开发者按住快捷键说话。
2. 系统自动识别语言并在悬浮窗角落显示“EN”或“中”图标。
3. 识别结果保持高准确率，结束后开发者双击悬浮窗文本直接插入到 IDE 的注释中。

### 用例 3：离线写作（V1.5）
**参与者**：在飞机上无网络的作家
**前置条件**：已提前下载中文本地模型 SenseVoice。
**流程**：
1. 作家打开写作软件，按住快捷键口述内容。
2. 音频数据送入本地引擎，延迟约 300ms 显示文字。
3. 全程无网络，所有数据保留在本机历史记录中。

---

## 8. 风险与依赖

| 风险项                       | 影响                         | 缓解措施                                                     |
| ---------------------------- | ---------------------------- | ------------------------------------------------------------ |
| 云 ASR 服务成本与稳定性      | 高额账单或服务中断影响体验   | 提供多家服务商适配，默认赠送免费额度；引导用户使用本地引擎。 |
| 本地模型识别率与性能不足     | 离线体验差                   | 采用经过优化的 Whisper.cpp 和 FunASR，提供量化模型选项。     |
| 系统快捷键冲突               | 用户无法正常唤醒             | 多备选快捷键方案，智能冲突检测，引导自定义。                 |
| Tauri 与操作系统兼容性更新   | 系统升级后某些 API 变动      | 及时跟随 Tauri 社区和系统 Beta 版本测试。                    |
| 音频设备多样性导致的采集问题 | 部分设备无法正常录音         | 提供多种音频后端（CPAL 优先，备选 cpal 备用），并加入设备选择功能。 |
| 用户对隐私的担忧             | 云端模式可能不被部分用户接受 | 明确数据传输加密，提供纯本地模式，不强制登录。               |

**依赖**：
- CPAL 音频库的跨平台支持与 Bug 修复。
- Whisper.cpp / FunASR 模型的流式支持更新。
- 第三方 ASR 云服务的 WebSocket 接口稳定性与定价。

---

## 9. 测试要点

### 功能测试
- 快捷键注册/注销在不同应用状态下的可靠性。
- 音频采集与流的完整性（16kHz mono PCM）。
- 各语言识别准确率与延迟。
- 显示窗口最大长度裁剪逻辑正确性。
- 动画性能与帧率测试。

### 压力测试
- 连续录音 1 小时，内存与 CPU 是否稳定。
- 快速切换语言和模型的鲁棒性。
- 同时运行多个其他实时应用（如视频会议）时的延迟表现。

### 安全测试
- 密钥存储是否加密。
- 网络传输抓包验证 WSS 加密。
- 本地数据库无明文敏感信息。

### 兼容性测试
- 至少覆盖 Windows 10、Windows 11、macOS 13、macOS 14（Apple Silicon 与 Intel 各一）。
- 测试不同麦克风设备（内置、USB、蓝牙耳机）。

---

## 10. 附录

### 10.1 目录结构详化
```
voiceflow/
├── src/                          # React 前端源码
│   ├── components/
│   │   ├── FloatingWindow/       # 悬浮窗核心组件
│   │   ├── Waveform/             # 动态波形
│   │   ├── Settings/             # 设置面板组件
│   │   ├── Shared/               # 通用 UI 组件
│   │   └── Animation/            # 动画配置
│   ├── stores/                   # Zustand 状态管理
│   ├── hooks/                    # 自定义 Hooks (useAudio, useASR)
│   ├── utils/                    # 工具函数
│   ├── styles/                   # Tailwind 全局样式
│   ├── App.tsx
│   └── main.tsx
├── src-tauri/                    # Rust 后端源码
│   ├── audio/
│   │   ├── capture.rs            # CPAL 音频捕获
│   │   ├── vad.rs                # 语音活动检测
│   │   └── mod.rs
│   ├── asr/
│   │   ├── engine_trait.rs       # ASR 引擎抽象接口
│   │   ├── streaming.rs          # WebSocket 流管理
│   │   ├── adapters/             # 各引擎适配器 (whisper, deepgram, funasr)
│   │   └── mod.rs
│   ├── shortcut/
│   │   ├── mod.rs                # 全局快捷键注册/注销
│   │   └── platform.rs           # 平台特定实现
│   ├── commands/                 # Tauri 命令
│   ├── db/                       # SQLite 数据库封装
│   ├── main.rs
│   └── Cargo.toml
├── public/                       # 前端静态资源
├── docs/                         # 设计文档与API文档
├── scripts/                      # 构建与开发脚本
├── package.json
└── README.md
```

### 10.2 术语表
- **ASR**：Automatic Speech Recognition，自动语音识别。
- **VAD**：Voice Activity Detection，语音活动检测。
- **CPAL**：Cross-Platform Audio Library，Rust 跨平台音频库。
- **Partial / Final**：流式识别中的中间结果与最终确认结果。
- **Tauri**：基于 Rust 的轻量级桌面应用框架。
- **Glassmorphism**：毛玻璃拟态设计风格。

